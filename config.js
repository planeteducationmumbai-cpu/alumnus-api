/* ============================================================
   ALUMNUS — Site configuration
   After deploying the backend, paste your API URL below.
   Leave empty to run in demo mode (no backend needed).
   ============================================================ */
window.ALUMNUS_API = "";   // e.g. "https://alumnus-api.onrender.com"
